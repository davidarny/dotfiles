[init]
  defaultBranch = main

[submodule]
  recurse = true
  fetchJobs = 16

[tag]
  forceSignAnnotated = false

[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
